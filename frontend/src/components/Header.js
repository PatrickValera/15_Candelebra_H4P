import { Box, Typography } from '@mui/material';
import React from 'react';

const Header = () => {
  return (
      <Box>
          <Typography varaint='h1'>Candelebra</Typography>
      </Box>
  )
};

export default Header;
